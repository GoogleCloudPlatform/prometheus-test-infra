apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-test
  namespace: prombench-{{ .PR_NUMBER }}
data:
  prometheus.yml: |
    global:
      scrape_interval: 30s

    scrape_configs:
    - job_name: kubelets
      scheme: https
      tls_config:
        ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        insecure_skip_verify: true
      bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
      kubernetes_sd_configs:
      - role: node
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_node_label_cloud_google_com_gke_nodepool]
        regex: prometheus-{{ .PR_NUMBER }}|nodes-{{ .PR_NUMBER }}
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)
      - target_label: __address__
        replacement: kubernetes.default.svc:443
      - source_labels: [__meta_kubernetes_node_name]
        regex: (.+)
        target_label: __metrics_path__
        replacement: /api/v1/nodes/${1}/proxy/metrics

    - job_name: node-exporters
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: node-exporter
      - action: replace
        source_labels: [__meta_kubernetes_service_name]
        target_label: job
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName

    - job_name: avalanche-1
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-2
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-3
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-4
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-5
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-6
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-7
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-8
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-9
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-10
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-11
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-12
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-13
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-14
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-15
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-16
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-17
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-18
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-19
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-20
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-21
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-22
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-23
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-24
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-25
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-26
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-27
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-28
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-29
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-30
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-31
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-32
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-33
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-34
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-35
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-36
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-37
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-38
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-39
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-40
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-41
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-42
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-43
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-44
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-45
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-46
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-47
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-48
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-49
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
    - job_name: avalanche-50
      kubernetes_sd_configs:
      - role: endpoints
        namespaces:
          names:
            - prombench-{{ .PR_NUMBER }}
      relabel_configs:
      - action: keep
        source_labels: [__meta_kubernetes_service_label_app]
        regex: avalanche
      - action: replace
        source_labels: [__meta_kubernetes_pod_node_name]
        target_label: nodeName
